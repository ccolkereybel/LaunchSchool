from add_point import add_point
from calculate_reflected_slope import calculate_reflected_slope


add_point(4, 3)
add_point(1, -9)
slope = calculate_reflected_slope()
print(slope)  # Outputs: -4.0